<div class="all-tags">
	<h2>Tags</h2>
	{{#foreach posts}}
		{{#foreach tags}}
	        <a href="{{url}}" title="{{name}}" class="btn alt small tag" data-id="{{id}}">{{name}}</a> 
	    {{/foreach}}
	{{/foreach}}
</div>